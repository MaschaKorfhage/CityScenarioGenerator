# CityScenarioGenerator

CityScenarioGenerator is a tool designed to create city scenarios for simulation with the LoadProfileGenerator. It extracts residential and non-residential buildings from BUILDA, maps them to corresponding categories in the LPG model, and saves everything in a directory of config files.
